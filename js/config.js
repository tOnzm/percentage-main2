// ── CONFIG ────────────────────────────────────────────────────────────────
// Global state — minimal surface area, kept here for cross-module access.

/** @type {'solid'|'gradient'|'studio'|'scene'|'custom'} */
let currentBgType = 'solid';
